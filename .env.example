# Postgres connection for the warehouse path. Optional --
# the pipeline falls back to data/processed/clean.parquet without it.
# No defaults are baked into the code: unset means unset.
PG_HOST=localhost
PG_PORT=5432
PG_DB=healthcare
PG_USER=
PG_PASSWORD=
